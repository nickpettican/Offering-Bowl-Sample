{
  "name": "offering-bowl",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "client",
    "api",
    "infra"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "docker compose --env-file .env up --build",
    "build:dev": "docker compose --env-file .env build --no-cache"
  },
  "keywords": [],
  "author": "nickpettican",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "husky": "^9.1.6"
  }
}
